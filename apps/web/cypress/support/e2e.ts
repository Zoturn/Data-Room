import "./commands";

// Shared setup for browser end-to-end specs. Commands that need a session arrive with
// add-authentication; this file exists so the harness is complete from the start.
export {};
