packages:
  - "apps/*"
  - "packages/*"

# Packages allowed to run install scripts (pnpm 10+ blocks them by default).
onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - argon2
  - esbuild
  - prisma
